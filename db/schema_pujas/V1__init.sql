-- Esquema propiedad de ms-pujas. No debe ser accedido directamente por otros microservicios (RNF-06).
-- Agregar aquí la tabla de pujas a medida que se defina.

CREATE SCHEMA IF NOT EXISTS schema_pujas;
